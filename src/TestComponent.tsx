import { Box, Typography } from "@mui/material";

export default function TestComponent() {
  return (
    <Box flex={1}>
      <Typography>A very boring test component</Typography>
    </Box>
  );
}
